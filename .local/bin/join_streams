#!/usr/bin/env bash


if [ $# -eq 3 ]; then
    ffmpeg -i "$1" -i "$2" -shortest -c:v copy -c:a aac -b:a 256k "$3"
else
    echo "Incorrect inputs"
    echo "Format:"
    echo "       join_streams INFILE.mp4 INAUDIO.mp3 OUTFILE.mp4"
fi